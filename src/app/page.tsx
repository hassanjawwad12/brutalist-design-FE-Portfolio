// Placeholder home — replaced in Phase 2 (foundation) by the Liquid Glass desktop.
// Kept minimal so the build stays green after the Phase 1 prune.
export default function Home() {
  return (
    <main
      style={{
        display: "grid",
        placeItems: "center",
        minHeight: "100dvh",
        fontFamily: "system-ui, sans-serif",
      }}
    >
      <p>Liquid Glass desktop — under construction.</p>
    </main>
  );
}
